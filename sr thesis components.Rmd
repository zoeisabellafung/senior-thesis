---
title: "sr thesis components"
author: "Zoe Fung"
date: "8/31/2022"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
# load required packages
library(readr)
library(ggplot2)
library(tibble)
library(ggmap)
library(janitor)
library(dplyr)
```

## data cleaning 

### fire severity data

#### fire locations
```{r}
# load USDA 1 dataset
USDAfires <- read.csv("data/USDA_RDA_fires_list.csv") %>%
  clean_names() %>% # standardize dataset variable names
  filter(state=="California") # subset dataset to inlcude only CA fires
# create list of fires in CA from USDA 1 dataset
CAfires <- distinct(USDAfires,yr_fire_name)

# load USDA 1 fire severity dataset
USDAseverity <- read.csv("data/USDA_RDA_pre_post_trees.csv") %>%
  clean_names() %>% # standardize dataset variable names
  filter(yr_fire_name==c(CAfires$yr_fire_name)) # subset dataset to include only CA fires

# create list of fires in CA that have species & fire severity data
CAfires_clean <- distinct(USDAseverity,yr_fire_name)

# filter USDA fires dataset to only include CA fires w/ location, species, & fire severity data
USDAfires_clean <- USDAfires %>%
  filter(yr_fire_name %in% CAfires_clean$yr_fire_name) 

# create a .csv file for USDAfires_clean
write.csv(USDAfires_clean, "USDAfires_clean.csv") # how do i specify a path for this file to be created in the data folder?

# create vectors for lat, long, and fire year/name
latitude <- c(37.279, 37.857, 38.805437, 36.874, 37.279722)
longitude <- c(-119.318, -120.086, -120.629714, -118.905, -119.50014)
yr_fire_name <- c("2013 - Aspen", "2013 - Rim", "2014 - King", "2015 - Rough", "2015 - Willow")

# create dataframe 
USDA2fires_clean <- as.data.frame(cbind(yr_fire_name,latitude,longitude))

# create a .csv file for USDA2fires_clean
write.csv(USDA2fires_clean, "USDA2fires_clean.csv")
```

#### pre-fire
```{r}
# load USDA 1 pre & post fire data
USDA1_prefire <- read.csv("data/USDA_RDA_pre_post_trees.csv") %>%
  clean_names() %>% # standardize column names
  filter(yr_fire_name %in% USDAfires_clean$yr_fire_name) %>% # subset data to only include CA fires w/ location, species, and fire severity data
  add_column(latitude=case_when(
    endsWith(yr_fire_name, "1989 - SEKI Crystal") ~ "36.60068",
    endsWith(yr_fire_name, "SEKI Grant West I") ~ "36.74582",
    endsWith(yr_fire_name, "SEKI Highway 2") ~ "36.57336",
    endsWith(yr_fire_name, "SEKI Tharps") ~ "36.56328",
    endsWith(yr_fire_name, "SEKI Deer Creek PNF") ~ "36.57336",
    endsWith(yr_fire_name, "SEKI Grant West") ~ "36.74582",
    endsWith(yr_fire_name, "LAVO Knob/ Lost Creek") ~ "40.56738",
    endsWith(yr_fire_name, "SEKI Hole-inthe-Wall") ~ "36.78695",
    endsWith(yr_fire_name, "SEKI Picnic Estates") ~ "36.79198",
    endsWith(yr_fire_name, "Crystal") ~ "39.51724",
    endsWith(yr_fire_name, "LAVO Butte Lake") ~ "40.58129",
    endsWith(yr_fire_name, "SEKI Mineral King") ~ "36.46600",
    endsWith(yr_fire_name, "WHIS Brandy Creek") ~ "40.61878",
    endsWith(yr_fire_name, "1996 - WHIS Shasta Divide") ~ "40.57916",
    endsWith(yr_fire_name, "REDW Mainstem") ~ "41.12729",
    endsWith(yr_fire_name, "REDW Midbasin") ~ "41.13717",
    endsWith(yr_fire_name, "Bucks") ~ "39.88539",
    endsWith(yr_fire_name, "SEKI Hercules") ~ "36.56936",
    endsWith(yr_fire_name, "SEKI Mineral KingS10") ~ "36.46601",
    endsWith(yr_fire_name, "Storrie") ~ "40.02744",
    endsWith(yr_fire_name, "Volcano Spring") ~ "39.15980",
    endsWith(yr_fire_name, "FSS2") ~ "36.59506",
    endsWith(yr_fire_name, "FSS5") ~ "36.58481",
    endsWith(yr_fire_name, "FSS6") ~ "36.58518",
    endsWith(yr_fire_name, "Southern Cascades Goosenest CA 174") ~ "41.54627",
    endsWith(yr_fire_name, "Southern Cascades Goosenest CA 178") ~ "41.56750",
    endsWith(yr_fire_name, "Southern Cascades Goosenest CA 179") ~ "41.58725",
    endsWith(yr_fire_name, "Southern Sierra Nevada Sequoia CA 1") ~ "36.59455",
    endsWith(yr_fire_name, "Southern Sierra Nevada Sequoia CA 4") ~ "36.58567",
    endsWith(yr_fire_name, "Southern Sierra Nevada Sequoia CA 5") ~ "36.58582",
    endsWith(yr_fire_name, "Star") ~ "39.09857",
    endsWith(yr_fire_name, "Swain") ~ "40.67535",
    endsWith(yr_fire_name, "Blodgett 129 - 60") ~ "38.91730",
    endsWith(yr_fire_name, "Blodgett 130 - 340") ~ "38.89821",
    endsWith(yr_fire_name, "Blodgett 131 - 400") ~ "38.89666",
    endsWith(yr_fire_name, "Cone") ~ "40.75172",
    endsWith(yr_fire_name, "McNalley") ~ "36.57336",
    endsWith(yr_fire_name, "REDW Copper Creek") ~ "41.15085",
    endsWith(yr_fire_name, "Southern Cascades Goosenest CA 181") ~ "41.60037",
    endsWith(yr_fire_name, "Southern Cascades Goosenest CA 182") ~ "41.56346",
    endsWith(yr_fire_name, "Southern Cascades Goosenest CA 183") ~ "41.55452",
    endsWith(yr_fire_name, "Southern Sierra Nevada Sequoia CA 2") ~ "36.59416",
    endsWith(yr_fire_name, "Southern Sierra Nevada Sequoia CA 3") ~ "36.59189",
    endsWith(yr_fire_name, "Southern Sierra Nevada Sequoia CA 8") ~ "36.57340",
    endsWith(yr_fire_name, "LABE South Boundary") ~ "41.69815",
    endsWith(yr_fire_name, "2003 - WHIS Shasta Divide") ~ "40.57916",
    endsWith(yr_fire_name, "Power") ~ "38.50190",
    endsWith(yr_fire_name, "Tennant") ~ "41.55805",
    endsWith(yr_fire_name, "Tharps2") ~ "36.56328",
    endsWith(yr_fire_name, "Harding") ~ "39.63345",
    endsWith(yr_fire_name, "SEKI Lewis Creek") ~ "36.76013",
    endsWith(yr_fire_name, "Angora") ~ "38.88529",
    endsWith(yr_fire_name, "Antelope") ~ "40.14120",
    endsWith(yr_fire_name, "Grass Valley") ~ "34.26739",
    endsWith(yr_fire_name, "PW-4") ~ "37.80125",
    endsWith(yr_fire_name, "American River Complex") ~ "39.22988",
    endsWith(yr_fire_name, "Peterson") ~ "40.89497",
    endsWith(yr_fire_name, "Piute") ~ "35.48923",
    endsWith(yr_fire_name, "Rich") ~ "40.06137",
    endsWith(yr_fire_name, "2008 - Seki Crystal") ~ "36.60068",
    endsWith(yr_fire_name, "WHIS White Rock") ~ "40.64606",
    endsWith(yr_fire_name, "Milford") ~ "40.10646",
    endsWith(yr_fire_name, "REDW Eastside") ~ "41.14705",
    endsWith(yr_fire_name, "Mountain") ~ "33.71030",
    endsWith(yr_fire_name, "Rim") ~ "37.90192",
    endsWith(yr_fire_name, "Salmon Complex") ~ "41.31864",
    endsWith(yr_fire_name, "Wooded Hills") ~ "32.85336"
  ))
```

### vegetation data
```{r}
# load vegetation data
vegetation <- read.csv("data/VTM_species_location.csv")
```

### plant trait data
```{r}
# load plant trait dataset
```

## map building
```{r}
# creating a sample data.frame with your lat/lon points
lon <- c(-38.31,-35.5)
lat <- c(40.96, 37.5)
df <- as.data.frame(cbind(lon,lat))

# getting the map
mapzoe <- get_map(location = c(lon = mean(df$lon), lat = mean(df$lat)), zoom = 4,
                      maptype = "satellite", scale = 2)

# plotting the map with some points on it
ggmap(mapzoe) +
  geom_point(data = df, aes(x = lon, y = lat, fill = "red", alpha = 0.8), size = 5, shape = 21) +
  guides(fill=FALSE, alpha=FALSE, size=FALSE)
```


