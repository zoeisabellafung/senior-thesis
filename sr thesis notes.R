# can't use google maps since need an api key...
# can i build a map in shiny instead of in rmd?

# to undo a commit: in terminal, type "git reset HEAD~"

# data processed so far:
  # USDA RDA 1981-2016 fire, pre-fire data
  # USDA RDA 2003-2017 fire data

# leaf traits to consider:
  # leaf area, leaf area per dry mass (SLA), leaf length (excluding petiole), leaf water content per dry mass, leaf litter decomposition rate constant